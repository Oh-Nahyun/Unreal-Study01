//
//  Study04(비트이동).cpp
//  CProject
//
//  Created by 오나현 on 11/19/24.
//

/*
#include <stdio.h>

/// 비트 이동 연산자 (<<, >>)
/// 시프트 : (뜻) 민다. / 쉐이더, 나나이트 등에서 비트 연산으로 처리
int main()
{
    unsigned char a = 4;    // 0000 0100
    unsigned char b = 8;    // 0000 1000
    printf("a : %d\n", a);
    printf("b : %d\n", b);
    
    a = 4 << 1;             // 0000 1000 (1비트를 밀어라)
    b = 8 << 2;             // 0010 0000 (2비트를 밀어라)
    printf("a : %d\n", a);
    printf("b : %d\n", b);
    
    unsigned char a2 = 1 << 8;       // 0000 0001 => 0000 0000 // 값이 날아감 (따라서, 자릿수 확인 중요!)
    unsigned int b2 = 1 << 8;        // 0000 0000 0000 0000 0000 0000 0000 0001 => 0000 0000 0000 0000 0000 0001 0000 0000
    printf("a2 : %d\n", a2);
    printf("b2 : %d\n", b2);
    
    return 0;
}
 */

#include <stdio.h>

int main()
{
    return 0;
}
